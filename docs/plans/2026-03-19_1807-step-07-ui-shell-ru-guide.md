# Step 07 Plan

- Refine the authenticated workspace shell into a more polished internal-tool layout with reusable patterns.
- Add Russian MVP UI copy for navigation, shell text, auth-visible labels, and placeholder workspace pages.
- Create protected placeholder routes plus an internal guide page, then verify the app still builds and protected routing behaves correctly.
