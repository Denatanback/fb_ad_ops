# Implementation Plan

- Create `docs/plans`, `docs/summary`, and `docs/changed_files` if they do not already exist.
- Update `AGENTS.md` so future Codex tasks must read `docs/roadmap/*`, preserve existing roadmap files unless explicitly asked, and write plan/summary/changed-files reports with Windows-safe timestamped filenames.
- Keep `docs/roadmap/backlog.md`, `docs/roadmap/implementation-plan.md`, and `docs/roadmap/mvp.md` unchanged.
