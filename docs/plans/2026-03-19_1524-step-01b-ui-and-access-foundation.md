# Implementation Plan

- Update `AGENTS.md` with non-negotiable guidance for the internal multi-user web service model, authentication, MVP workspace assumptions, and UI direction.
- Add `docs/product/access.md` to define the MVP access model for authenticated internal users with `admin` and `member` roles.
- Add `docs/ux/ui-direction.md` and make a small update to `docs/dev/decisions.md` so future implementation stays consistent with the intended internal-tool UI.
- Keep all existing files in `docs/roadmap` unchanged.
