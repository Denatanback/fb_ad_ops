# Plan

- Upgrade the Telegram notifier from a single shared-chat send helper to a topic-aware forum supergroup notifier with env-driven topic resolution.
- Update the protected admin notifications test flow so an admin can choose a destination topic and send a real test message through the new routing layer.
- Refresh docs and env examples, then verify the build and the protected route behavior.
