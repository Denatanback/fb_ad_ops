# Coding Rules

- Prefer small functions and clear naming
- Keep domain types explicit
- Validate all create/update inputs
- Keep UI logic separate from persistence logic where practical
- Avoid hidden domain assumptions
